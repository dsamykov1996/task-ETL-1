# 📘 Опис виконаної роботи

## ✔ Що було зроблено
У ході аналізу та обробки даних було виконано:

1. Завантаження сирого датасету та проведення базового огляду:
   - `head()`, `info()`, `describe()`
   - аналіз пропусків та дублікатів
   - перегляд списку колонок

2. Очищення даних:
   - видалення зайвих пробілів і стандартизація тексту
   - нормалізація email у нижній регістр
   - очищення телефонних номерів (залишені тільки цифри та можливий "+")
   - вирівнювання регістру для імен, міст, адрес

3. Створення нових ознак (Feature Engineering):
   - `full_name` — об’єднання імені та прізвища
   - `email_domain` — домен з email
   - `city_length` — довжина назви міста
   - `is_gmail` — ознака Gmail-користувача

4. Вибірки даних:
   - користувачі Gmail
   - компанії з “LLC” / “LTD”
   - позиційні вибірки (`iloc`)
   - випадкові вибірки (`sample`)

5. Групування та статистика:
   - кількість людей у кожному місті
   - кількість унікальних email-доменів по містах
   - ТОП-5 міст
   - ТОП-5 email-доменів
   - агрегування через `groupby()` + `agg()`

6. Експорт результатів:
   - `cleaned.csv` — очищений датасет
   - `gmail_users.csv` — вибірка Gmail-користувачів
   - `tops.xlsx` — Excel файл із двома вкладками (ТОП міст та доменів)

---

## ⚠ Які проблеми знайдено в датасеті
Під час аналізу були виявлені наступні недоліки:

- нестандартизовані текстові значення (різні регістри, зайві пробіли)
- неуніфіковані email-адреси
- телефонні номери містили зайві символи
- поодинокі пропущені значення
- потенційні дублікати записів
- різний формат назв міст та компаній

---

## ⭐ Найкорисніші трансформації
- Стандартизація тексту → покращило групування та пошук
- Очищення телефонних номерів → уніфікований формат
- Виділення `email_domain` → стало можливим робити ТОП доменів
- Створення `is_gmail` → швидкі вибірки конкретного сегмента
- Агрегація через `agg()` → кілька метрик по містах в одному кроці

---

## 🔍 Цікаві вибірки та висновки
- Gmail виявився найпоширенішим email-доменом.
- Виділяються міста з особливо великою кількістю записів — вони формують ТОП-5.
- Компанії типу “LLC” та “LTD” зустрічаються набагато частіше у певних регіонах.
- Довжина назви міста не має аномальних значень — дані чисті.
- Email-домени добре піддаються сегментації — можуть бути корисні для маркетингу.

---

## 🚀 Що можна покращити у майбутньому
1. Додати бібліотеку для валідації телефонів (`phonenumbers`).
2. Нормалізувати назви міст через словник (Kyiv/Kiev → Kyiv).
3. Додати геолокацію міст та побудувати географічні карти.
4. Побудувати детальніший аналіз email-доменів.
5. Виявляти аномалії або нетипові значення.
6. Оформити обробку даних як автоматизований pipeline або клас.

